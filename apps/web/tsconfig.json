{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["DOM", "DOM.Iterable", "ES2023"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "preserve",
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "incremental": true,
    "allowJs": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    "plugins": [{ "name": "next" }]
  },
  "// typecheck note": "Deliberately NOT including .next/types: the deploy runs `tsc --noEmit` BEFORE `next build`, so stale generated route-types from a prior build (e.g. a route since deleted) would break typecheck. `next build` validates those generated types itself, so no coverage is lost.",
  "include": ["next-env.d.ts", "src"],
  "exclude": ["node_modules", ".next"]
}
